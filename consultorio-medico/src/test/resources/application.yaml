spring:
  datasource:
    url: jdbc:mysql://localhost:3306/consultingroomiwa
    username: root
    password: 1234
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

  jpa.hibernate.ddl-auto: create-drop